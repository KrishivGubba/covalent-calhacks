{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dashboard",
  "description": "Capability for the dashboard window with update functionality",
  "windows": ["dashboard"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default",
    "notification:default",
    "process:default"
  ]
}
