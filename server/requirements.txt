# Flask Server Requirements
flask>=2.3.0
flask-cors>=4.0.0
requests>=2.31.0
python-dotenv>=1.0.0
anthropic>=0.71.0
scikit-learn
PyYAML
pytz
google-auth
google-auth-oauthlib
google-auth-httplib2
fastmcp

# Database Encryption
# SQLCipher: use sqlcipher3-wheels for macOS/Windows (pre-built); or brew install sqlcipher + pip install pysqlcipher3
sqlcipher3-wheels>=0.5.0
keyring>=24.0.0           # macOS Keychain integration for secure key storage

# Agent-S Dependencies (installed separately from Agent-S-fork)
# See ../Agent-S-fork/requirements.txt for complete Agent-S dependencies
pyautogui>=0.9.54
pillow>=10.0.0

# Analytics
posthog>=3.0.0

# Logging and utilities
# (anyhow is a Rust crate, not a Python package)


